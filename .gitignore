# Secrets & env
*.env
!*.example.env
.env.local
.env.secrets
.pgpass
*.key
*.pem

# PostgreSQL artifacts
*.dump
*.backup
*.sql.gz
*.tar.zst
pg_log/
pgdata/

# Build / test outputs
*.tmp
*.log
# …except the test fixtures, which are input, not output. Without this the
# suite passes locally (the file is present but untracked) and fails in CI on a
# fresh checkout — the worst kind of difference between the two.
!tests/fixtures/*.log
test-output/
coverage/
.coverage
.coverage-html/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary
/tmp/
*.bak
*.orig

# Personal / marketing content (not for upstream)
REDDIT_POST.md
LINKEDIN_POST.md
NOTES.local.md

# Internal anonymisation tooling (do not publish)
scripts/_internal/

# Nextcloud / rclone symlinks
*.rclonelink

# Claude Code working directory (slash commands, settings, caches).
# NOTE: CLAUDE.md is deliberately NOT ignored — it is a shipped artifact of this
# repository (see README "Claude Code workflow blueprint"), not a local override.
.claude/
