# PostgreSQL Schema-Definitionen

Schema- und Extension-Definitionen für die PostgreSQL-Migration.

## Geplante Dateien
- `00_extensions.sql` - PostgreSQL Extensions (pg_cron, etc.)
- `01_schema.sql` - CREATE SCHEMA order_processing
- `02_types.sql` - Custom Types (falls benötigt)
